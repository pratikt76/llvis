{
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "rewrites": [
        {
            "source": "/llvis/(.*)",
            "destination": "https://llvis.vercel.app/"
        }
    ]
}
